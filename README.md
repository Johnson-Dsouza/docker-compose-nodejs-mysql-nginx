# Docker-compose-NodeJs-Nginx-MySQL

Using docker compose to create an environment with nodejs, mysql and nginx

## Command to run containers:

```
docker-compose up -d
```

The application will be running on http://localhost:8080